
JS题解
```
var maxSubArray = function(nums) {
    let mimo = [];
    mimo[0] = nums[0];
    let max = mimo[0];
    for(let i = 1 ;i<nums.length; i++ ){
        mimo[i] = Math.max(nums[i]+mimo[i-1],nums[i]);
        max = Math.max(max,mimo[i]);
    }
    return max;

};
```
